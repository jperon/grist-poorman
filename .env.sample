DOMAIN='grist.example.com'
URL='https://grist.example.com'
ORG='example'
EMAIL='you@example.com'
TELEMETRY='limited'
DEBUG=1
HTTPS='auto'
STAGING='true'  # Don't forget setting this to false in production!
#HTTPS='manual'
# Self-signed fallback certs (used when HTTPS=auto, overridden by ACME at runtime).
# For HTTPS=manual, point to your real certificate and key.
SSL_CERT=./ssl/cert.pem
SSL_KEY=./ssl/key.pem
#WIDGETS_URL='https://grist-widgets.example.com'